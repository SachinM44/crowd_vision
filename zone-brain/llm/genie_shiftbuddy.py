"""genie_shiftbuddy.py — offline shift-buddy Q&A (SHOULD-HAVE, open only if G3 passes).

OWNER: Alpha (TODO(alpha)). STUB.

Genie bundle (open model) via onnxruntime-genai (note: `-genai-qnn` does NOT
exist), exported pre-event in WSL2. Off the safety path. Badged litert-npu /
qnn depending on the export. Do not build unless the core is green (G3).
"""
from __future__ import annotations


def ask(question: str) -> str:
    raise NotImplementedError("TODO(alpha): Genie bundle Q&A (should-have)")
