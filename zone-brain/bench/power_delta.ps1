# power_delta.ps1 — battery-delta per power profile (BENCHMARKS.md #8).
# OWNER: Alpha (TODO(alpha)). STUB.
#
# powercfg /batteryreport before/after two 10-min runs: burst vs balanced (mesh
# running). Emit JSON -> bench/out/power.json for the BENCH:power marker.
Write-Host "TODO(alpha): powercfg /batteryreport burst-vs-balanced -> bench/out/power.json"
exit 1
