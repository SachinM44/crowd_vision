"""venue-tier/sarvam_adapter.py — EMPTY SLOT (adopt only if offered).

OWNER: Gamma. Sarvam session is Sat 11:30-12:00 (Delta listens). If an API/key or
on-device Indic model is offered, this upgrades template_fallback to
trilingual-on-edge and re-badges advisories "sarvam-edge". Adopt ONLY if <=90 min
integration and the core (G2) is green — decided at G2. Until then: intentionally
empty.
"""
from __future__ import annotations

AVAILABLE = False  # flip to True only if adopted at G2
