# --- Model weights & AI artifacts (Rules §8.c/§8.d — never commit) ----------
*.onnx
*.dlc
*.litertlm
*.pte
*.pt
*.caffemodel
*.bin
weights/
models/

# --- Qualcomm runtime binaries (redistribution-restricted) ------------------
libQnn*
QnnHtp.dll
libLiteRtDispatch_Qualcomm.so
libQnnSystem.so
libGemmaModelConstraintProvider.so

# --- Secrets ---------------------------------------------------------------
.env
*.key

# --- Confidential build docs — NEVER push (team-internal PDFs) -------------
# This section was EMPTY, so resource/ was still tracked. The hackathon rules
# (§17) make status/progress material confidential and the repo goes public.
resource/
*.pdf

# --- Android build artifacts -----------------------------------------------
*.apk
*.aab
*.keystore
.gradle/
local.properties
field-app/app/libs/
field-app/app/src/main/jniLibs/

# --- Python ----------------------------------------------------------------
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.pytest_cache/
.venv/
venv/

# --- Node / frontend -------------------------------------------------------
node_modules/

# --- Bench outputs (regenerated; JSON embedded into BENCHMARKS.md) ---------
# **/ so zone-brain/bench/out/ is covered too (a bare bench/out/ only matched
# the repo root — that is how mesh.json got tracked).
**/bench/out/
*.bench.json
.kotlin/

# --- OS / editor -----------------------------------------------------------
.DS_Store
Thumbs.db
.vscode/
.idea/
